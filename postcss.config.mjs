/**
 * @file PostCSS build configuration enabling the Tailwind CSS plugin.
 * Transforms project styles during Next.js builds; contains no runtime game logic.
 */
export default { plugins: { "@tailwindcss/postcss": {} } };
